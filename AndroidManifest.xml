<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bsb.hike"
    android:versionCode="4"
    android:versionName="0.0.4" >

    <uses-sdk android:minSdkVersion="7" />
    <uses-permission android:name="android.permission.READ_CONTACTS"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" 
        android:name="com.bsb.hike.HikeMessengerApp"
        android:debuggable="true">

        <service
            android:name="HikeService"
            android:enabled="true">            
        </service>

        <activity
            android:name=".ui.MessagesList"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar"

            >
            <intent-filter >
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:label="@string/app_name"
            android:name=".ui.WelcomeActivity" >
        </activity>
        <activity
            android:label="@string/app_name"
            android:name=".ui.AccountCreateSuccess">
        </activity>
        <activity
            android:label="@string/app_name"
            android:name=".ui.ScanningAddressBook">
        </activity>
        <activity
            android:windowSoftInputMode="stateHidden"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar"
            android:name=".ui.ChatThread">
        </activity>
    </application>

</manifest>